{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": [
        "^build"
      ],
      "outputs": [
        "bin/**"
      ]
    },
    "//#build:root": {
      "outputs": [
        "lib/**"
      ]
    },
    "test": {
      "dependsOn": [
        "^build"
      ],
      "outputs": [
        "bin/**"
      ]
    },
    "test:bun": {
      "dependsOn": [
        "^build"
      ],
      "outputs": [
        "bin/**"
      ]
    }
  }
}
